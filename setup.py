import json
import os
from setuptools import setup

setup(
    name='css-generator',
    version='0.0.1',
    author='Hugo Paquet',
    packages=['css-generator'],
    include_package_data=True,
    license='MIT',
    description='',
    install_requires=[],
    classifiers=[
    ],    
)
